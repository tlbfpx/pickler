## REMOVED Requirements

### Requirement: Spec 2 declares placement input contract

**Reason:** Spec 2 declared a placeholder requirement that placement issuance would consume the final standings. Spec 3 now makes that real, so the placeholder is no longer needed.

**Migration:** No code action; this is a spec-level placeholder being replaced by the real placement capability in `specs/placement/spec.md`.